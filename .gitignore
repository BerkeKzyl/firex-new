# ❌ Node.js bağımlılıkları
node_modules/

# ❌ Next.js geçici build klasörü
.next/

# ❌ Build çıktısı
out/

# ❌ Ortam değişkenleri (şifreler, API keyler burada olur)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.next.config.ts

# ❌ log dosyaları
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ❌ macOS için özel dosyalar
.DS_Store

# ❌ VS Code ayarları (projeye özel değilse gerek yok)
.vscode/

# ❌ Git stash cache'i
*.orig

# ❌ lock dosyası (proje sadece yarn veya npm kullanıyorsa diğerini dışla)
package-lock.json
# veya
yarn.lock

# ❌ coverage raporları (jest vs. varsa)
coverage/

# ❌ Türkçe karakterli klasör/dosya isimleri varsa önlem olarak (isteğe bağlı)
Thumbs.db
ehthumbs.db
